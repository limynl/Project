<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/add_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ListView
            android:id="@+id/jws_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:cacheColorHint="#fafafa"
            android:divider="@null"
            android:scrollbars="none" />

    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/jws_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="55dp"
        android:layout_marginRight="27dp"
        android:clickable="true"
        android:src="@drawable/ic_logo"
        app:backgroundTint="@color/colorTheme"
        app:fabSize="normal"
        app:layout_anchor="@id/add_refresh_layout"
        app:layout_anchorGravity="bottom|right"
        app:rippleColor="@color/colorTheme" />

</android.support.design.widget.CoordinatorLayout>
